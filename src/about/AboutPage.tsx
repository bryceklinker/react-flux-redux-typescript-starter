import * as React from 'react';

export const AboutPage = (props) => {
    return (
        <div>
            <h1>About</h1>
            <p>This application use React, Redux, React Router, and a variety of other helpful libraries</p>
        </div>
    );
};
